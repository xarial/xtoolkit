<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Xarial.XToolkit.Wpf.Controls">
    
    <ControlTemplate x:Key="NumberBox" TargetType="{x:Type Control}">
        <TextBox x:Name="PART_TextBox" Background="{TemplateBinding Background}"
                 BorderBrush="{TemplateBinding BorderBrush}"
                 BorderThickness="{TemplateBinding BorderThickness}">
        </TextBox>
    </ControlTemplate>

    <Style TargetType="{x:Type local:NumberBoxDouble}">
        <Setter Property="Template" Value="{StaticResource NumberBox}"/>
    </Style>

    <Style TargetType="{x:Type local:NumberBoxInteger}">
        <Setter Property="Template" Value="{StaticResource NumberBox}"/>
    </Style>
</ResourceDictionary>